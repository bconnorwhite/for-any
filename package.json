{
  "name": "@bconnorwhite/for-any",
  "version": "1.4.0",
  "description": "Use map, filter, reduce, and forEach on both arrays and non-array types.",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "rebuild": "rimraf ./build && yarn build"
  },
  "author": "Connor White",
  "license": "ISC",
  "keywords": [
    "for",
    "any",
    "forEach",
    "map",
    "reduce",
    "filter",
    "item",
    "array",
    "typescript"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ConnorWhite/for-any.git"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
